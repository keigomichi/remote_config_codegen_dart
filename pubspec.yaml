name: remote_config_codegen
description: Generate typed Dart Remote Config clients and JSON models from a JSON manifest.
version: 0.2.0
repository: https://github.com/keigomichi/remote_config_codegen_dart
environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  args: ^2.7.0
  meta: ^1.19.0
  yaml: ^3.1.3

dev_dependencies:
  frontend_server_client: ^4.0.0
  test: ^1.30.0

executables:
  remote_config_codegen: remote_config_codegen
